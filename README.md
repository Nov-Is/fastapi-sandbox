# FastAPI Learning

FastAPI の学習用プロジェクトです。

## 起動方法

Docker Compose でビルド・起動します。

```bash
docker compose up --build
```

## Swagger UI

起動後、以下の URL にアクセスすると Swagger UI で API を確認・操作できます。

http://localhost:8000/docs
